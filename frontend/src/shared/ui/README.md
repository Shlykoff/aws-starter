This folder is generated by the shadcn CLI (`yarn dlx shadcn@latest add <component>`, see `components.json`) and may be edited freely.
The CLI writes `import { cn } from "cn"` (an extra npm package); after adding a component, change it to `@/shared/lib/utils`.
Only the components the app uses live here; add more with the CLI when needed.
