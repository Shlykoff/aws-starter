# Test and lint tools, on top of the runtime dependencies (httpx, which Starlette's test client
# also uses, is a runtime dependency: it sends the webhook).
-r requirements.txt

# Direct
pytest==9.1.1
ruff==0.16.8

# Pulled in by pytest
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
